{
  "name": "montgomery-business-companion-explorer",
  "private": true,
  "version": "1.0.0",
  "description": "Spatial Census Explorer for Montgomery, AL with hierarchical Opportunity and Attractiveness Scoring",
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'No build step required'",
    "lint": "echo 'No lint step required'"
  },
  "dependencies": {
    "@google/genai": "^2.0.0"
  }
}
